<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Upon_Conversion_Check_to_Ensure_Fields</fullName>
    <active>true</active>
    <description>are complete</description>
    <errorConditionFormula>AND(
     ISPICKVAL(Division__c , &quot;VPS&quot;),
    IsConverted, 
   OR( 
        ISBLANK(FirstName), 
        ISBLANK (LastName), 
        ISBLANK(Email), 
        ISBLANK(Company)
    )
)</errorConditionFormula>
    <errorMessage>Ensure, First Name, Last Name, Email and Company are Completed.</errorMessage>
</ValidationRule>
